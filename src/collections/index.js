const {
  createCollection,
  findCollection,
  addFilesToCollection,
  addLinksToCollection,
} = require('./actions');

module.exports = {
  createCollection,
  findCollection,
  addFilesToCollection,
  addLinksToCollection,
};
